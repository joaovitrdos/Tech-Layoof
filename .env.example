# Copie para .env e preencha os valores. O .env e carregado por
# spring.config.import=optional:file:.env[.properties] (application.properties).

# ---------- Datasource ----------
DB_URL=jdbc:postgresql://localhost:5432/example
DB_USERNAME=example
DB_PASSWORD=exaple

# ---------- JWT ----------
# Obrigatorio: sem valor a aplicacao nao sobe.
LAYOOF_JWT_SECRET=a7K9-X2mP-4vR1-t8N6-Qz3w-9FjY-c5H2-Lp0T-6dE4-vB7mX

# ---------- Google OAuth ----------
GOOGLE_CLIENT_ID=

# ---------- E-mail (SMTP) ----------
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true
MAIL_ENABLED=true
